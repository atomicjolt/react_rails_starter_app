{
  "name": "tmp",
  "version": "0.0.1",
  "description": "Javascript for the Tmp.",
  "engines": {
    "node": "0.10.32"
  },
  "scripts": {
    "postinstall": "if [ \"$RAILS_ENV\" = \"production\" ] ; then cd ./client && npm install --production; else cd ./client && npm install; fi",
    "gulp": "cd ./client && npm run gulp",
    "test": "rspec && client/bin/lint && (cd client && npm run jscs .)"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/atomicjolt/tmp"
  },
  "keywords": [
    "react",
    "Instructure Canvas",
    "Rails",
    "LTI",
    "OAuth"
  ],
  "author": "atomicjolt",
  "license": "MIT",
  "homepage": "https://github.com/atomicjolt/tmp",
  "dependencies": {
  }
}
